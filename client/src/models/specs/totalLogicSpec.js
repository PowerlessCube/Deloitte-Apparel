var TotalLogic = require( "../totalLogic.js" );
var assert = require( "chai" ).assert;

describe("totals math log", function() {

	it('should return 0', function() {
		assert.equal( 0, Logic.ItemSum );
	});

});
